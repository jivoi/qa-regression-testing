pygments has a test suite in the tests/ directory. How to run in a vm:

$ sudo apt-get build-dep pygments
$ sudo apt-get install python-nose
$ apt-get source pygments
$ cd pygments*/tests
$ python ./run.py

